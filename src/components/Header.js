import React from 'react';
import { StyledHeader } from './styled/Header.styled';

export default function Header(){

    return(
        <StyledHeader>
            <h2>is it raining?</h2>
            <p><small>React App created by Brad Hodge</small></p>
        </StyledHeader>
    )
}